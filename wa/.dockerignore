# The live WhatsApp login. Never bake this into an image — it is a credential.
# It is a runtime volume instead (see docker-compose.yml).
wa_profile/
service_account.json
links.csv
*.csv
qr.png
debug.png

# local history + build inputs the image does not need
checkpoints/
ui-src/
.venv/
.venv-build/
build/
dist/
node_modules/
__pycache__/
*.pyc
.DS_Store
.git/
