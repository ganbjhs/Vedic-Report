"""Facebook capture engine — see fb_capture.py. Parallel to src/ and
influencer/ (RULEBOOK rule 18); invoked through the profile engine
(`profiles/run_profile.py --profile facebook`), never through run.py."""
