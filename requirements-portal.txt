# Client Portal dependencies — SEPARATE from requirements-web.txt so the
# portal image stays small (no Playwright, no reportlab, no Pillow).
#
#   pip install -r requirements-portal.txt
#   python -m uvicorn portal.main:app --port 8020

fastapi>=0.110,<1.0        # web framework
uvicorn[standard]>=0.27,<1.0   # ASGI server
jinja2>=3.1,<4.0           # server-rendered templates
python-multipart>=0.0.9    # form posts (login, invite)
openpyxl>=3.1,<4.0         # the Excel export
python-dotenv>=1.0,<2.0    # reads .env when run by hand (optional)
