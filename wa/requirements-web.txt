# Server build only. The desktop app keeps using requirements.txt (which has
# pywebview); a server has no window manager, so pywebview is deliberately absent.
#
# playwright is PINNED to the version baked into the base image in Dockerfile —
# the browser binaries live in the image, and pip upgrading the client past them
# is the classic "Executable doesn't exist" failure.
playwright==1.61.0
gspread>=6.0
requests>=2.31

# the HTTP layer
fastapi>=0.110
uvicorn[standard]>=0.27
itsdangerous>=2.1          # signs/reads the shared ra_session cookie
python-multipart>=0.0.9    # /upload, the replacement for the native file dialog
