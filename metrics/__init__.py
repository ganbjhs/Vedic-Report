"""Engagement metadata readers — numbers only, no screenshots.

Its own folder for the same reason `facebook/` and `instagram/` have theirs
(RULEBOOK rule 18): a reader that visits a post and comes back with counts is a
different job from a capture that comes back with pixels, and mixing the two is
how `src/` stopped being frozen the first time.
"""
